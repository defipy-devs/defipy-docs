# Brief 1 — IA demolition + skeleton (Day 5 morning)

**Target repo:** `/Users/ian_moore/repos/defipy-docs`
**Branch:** `v2` (already checked out — confirm with `git branch --show-current` before starting)
**Scope:** Rewrite the Sphinx toctree and captions, drop dropped sections, create stub files for new sections, preserve URLs that the plan calls out for preservation. Produce a clean local build with empty new pages and a correctly-reorganized sidebar.

**Explicit non-goals:**
- Do not rewrite `index.rst` prose above the toctree. Tagline, intro paragraphs, "Primitive Interface vs Abstract Interface" recommendation, numbered object descriptions (Join/Swap/…), the Precision table — all of that stays as-is. Only the toctrees at the bottom of `index.rst` get rewritten.
- Do not write content for the new sections. Only stub pages with a one-line placeholder.
- Do not touch `defipy-docs/README.rst`.
- Do not run autodoc yet.

---

## 0. Pre-flight

```bash
cd /Users/ian_moore/repos/defipy-docs
git branch --show-current    # expect: v2
git status                   # expect: clean working tree
```

If the branch isn't `v2` or the tree isn't clean, stop and ask.

Install build dependencies if not already present (Ian confirms all tools are installed, but verify):

```bash
pip install -r docs/requirements.txt
```

Update `docs/requirements.txt` **before** the rest of the work — the `defipy >= 0.0.19` pin is stale. Change to `defipy >= 2.0.0`. Do not reformat the rest of the file (the pip-compile autogenerated section is fragile; just edit the one line).

Establish a local build baseline before any edits:

```bash
cd docs
sphinx-build -b html . _build/html 2>&1 | tee /tmp/build_baseline.log
cd ..
```

Keep `/tmp/build_baseline.log` for reference. If the baseline build fails, stop and ask — we need a working baseline to judge our edits against.

---

## 1. File deletions

Delete the Onchain section files. Ian has these captured in book tutorials; they are leaving the SDK docs entirely.

```bash
git rm -r docs/onchain/
```

Delete the cruft PDF:

```bash
git rm "docs/xdm_business_plan.pdf"
```

(The filename may have different casing on disk — use `ls docs/*.pdf` to find the exact name. Delete whichever XDM-related PDF is present.)

Commit the deletions as one step before continuing:

```bash
git add -A
git commit -m "chore(docs): drop onchain section and xdm PDF ahead of v2 reorg"
```

---

## 2. Fix `conf.py`

Two edits, both in `/Users/ian_moore/repos/defipy-docs/docs/conf.py`.

**Edit A — resolve the nbsphinx / exclude_patterns contradiction.**

Current state (lines ~30-42):

```python
extensions = [
    ...
    "nbsphinx",  # temporarily disabled due to nbconvert issues
    ...
]

exclude_patterns = ["_build", "Thumbs.db", ".DS_Store", "**/.ipynb_checkpoints", "**/*.ipynb"]
```

And further down (line ~58):

```python
exclude_patterns = ["_build", "Thumbs.db", ".DS_Store", "**/.ipynb_checkpoints"]
```

The file has **two** `exclude_patterns` assignments — the second silently overrides the first. For v2.0, keep notebooks excluded (per the agreed decision). Resolution:

1. Remove `"nbsphinx"` from the `extensions` list (it's declared but we're not rendering notebooks — having it active while excluding notebooks is inconsistent)
2. Keep a single `exclude_patterns` line that excludes `.ipynb` files explicitly
3. Delete the duplicate `exclude_patterns` assignment further down

Target state:

```python
extensions = [
    "sphinx.ext.duration",
    "sphinx.ext.doctest",
    "sphinx.ext.autodoc",
    "sphinx.ext.autosummary",
    "sphinx.ext.intersphinx",
    "sphinx_design",
    "sphinx_sitemap",
    "sphinxcontrib.googleanalytics",
]

exclude_patterns = [
    "_build",
    "Thumbs.db",
    ".DS_Store",
    "**/.ipynb_checkpoints",
    "**/*.ipynb",
]
```

Also remove the `# -- nbsphinx configuration --` block (the `nbsphinx_prompt_width = "0"` line) since nbsphinx is no longer an extension.

**Edit B — do NOT change any of the following:**
- `project`, `copyright`, `author` — unchanged
- `html_baseurl` — unchanged (`https://defipy.readthedocs.io/en/latest/`)
- `html_theme` — stays `sphinx_rtd_theme`
- `html_theme_options` — unchanged
- `html_meta` — unchanged
- `html_logo` — unchanged
- `googleanalytics_id` — unchanged
- `intersphinx_mapping` — unchanged

No theme change, no visual rebrand. Phase 1 IA only.

---

## 3. Create stub files for new pages

Create the following files. Each file contains a single RST title plus a one-line placeholder. Use the exact content shown for each.

### 3a. Getting Started — What's new in v2.0

**File:** `docs/quick/whats_new_v2.rst`

```rst
.. _whats_new_v2:

What's new in v2.0
====================

*Page content coming in Brief 2. This stub exists so the toctree builds clean.*
```

### 3b. DeFi Math — Balancer and Stableswap stubs

**File:** `docs/math/balancer_math.rst`

```rst
.. _balancer_math:

Balancer Math
====================

*Page content coming in Brief 2. This stub exists so the toctree builds clean.*
```

**File:** `docs/math/stableswap_math.rst`

```rst
.. _stableswap_math:

Stableswap Math
====================

*Page content coming in Brief 2. This stub exists so the toctree builds clean.*
```

### 3c. Agentic DeFi section — 5 pages

Create directory `docs/agentic/`.

**File:** `docs/agentic/index.rst`

```rst
.. _agentic_index:

Agentic DeFi
====================

.. toctree::
   :maxdepth: 1

   overview
   tool_schemas
   binding_to_claude
   binding_to_other_llms
   mcp_demo

*Page content coming in Brief 2. This stub exists so the toctree builds clean.*
```

**File:** `docs/agentic/overview.rst`

```rst
.. _agentic_overview:

Overview
====================

*Page content coming in Brief 2. This stub exists so the toctree builds clean.*
```

**File:** `docs/agentic/tool_schemas.rst`

```rst
.. _tool_schemas:

Tool Schemas (defipy.tools)
============================

*Page content coming in Brief 2. This stub exists so the toctree builds clean.*
```

**File:** `docs/agentic/binding_to_claude.rst`

```rst
.. _binding_to_claude:

Binding to Claude
====================

*Page content coming in Brief 2. This stub exists so the toctree builds clean.*
```

**File:** `docs/agentic/binding_to_other_llms.rst`

```rst
.. _binding_to_other_llms:

Binding to Other LLMs
========================

*Page content coming in Brief 2. This stub exists so the toctree builds clean.*
```

**File:** `docs/agentic/mcp_demo.rst`

```rst
.. _mcp_demo:

MCP Server Demo Walkthrough
=============================

*Page content coming in Brief 2. This stub exists so the toctree builds clean.*
```

### 3d. State Twin section — 4 pages

Create directory `docs/twin/`.

**File:** `docs/twin/index.rst`

```rst
.. _twin_index:

State Twin
====================

.. toctree::
   :maxdepth: 1

   concept
   mock_provider
   live_provider
   custom_twins

*Page content coming in Brief 2. This stub exists so the toctree builds clean.*
```

**File:** `docs/twin/concept.rst`

```rst
.. _twin_concept:

Concept
====================

*Page content coming in Brief 2. This stub exists so the toctree builds clean.*
```

**File:** `docs/twin/mock_provider.rst`

```rst
.. _mock_provider:

MockProvider
====================

*Page content coming in Brief 2. This stub exists so the toctree builds clean.*
```

**File:** `docs/twin/live_provider.rst`

```rst
.. _live_provider:

LiveProvider (v2.1)
====================

*Page content coming in Brief 2. This stub exists so the toctree builds clean.*
```

**File:** `docs/twin/custom_twins.rst`

```rst
.. _custom_twins:

Building Custom Twins
=======================

*Page content coming in Brief 2. This stub exists so the toctree builds clean.*
```

### 3e. Agentic Primitives section — 13 pages

Create directory `docs/agentic_primitives/`.

**File:** `docs/agentic_primitives/index.rst`

```rst
.. _agentic_primitives_index:

Agentic Primitives
====================

.. toctree::
   :maxdepth: 1

   primitive_contract
   position_analysis
   price_scenarios
   pool_health
   risk
   optimization
   comparison
   execution
   portfolio
   break_even
   tools_reference
   twin_reference
   result_dataclasses

*Page content coming in Brief 2 (narrative text) and Brief 3 (autodoc). This stub exists so the toctree builds clean.*
```

Then create each of the following files with the same "*Page content coming in Brief 2/3…*" placeholder, adjusting the title to match the toctree entry:

- `docs/agentic_primitives/primitive_contract.rst` — title `The Primitive Contract`
- `docs/agentic_primitives/position_analysis.rst` — title `Position Analysis`
- `docs/agentic_primitives/price_scenarios.rst` — title `Price Scenarios`
- `docs/agentic_primitives/pool_health.rst` — title `Pool Health`
- `docs/agentic_primitives/risk.rst` — title `Risk`
- `docs/agentic_primitives/optimization.rst` — title `Optimization`
- `docs/agentic_primitives/comparison.rst` — title `Comparison`
- `docs/agentic_primitives/execution.rst` — title `Execution`
- `docs/agentic_primitives/portfolio.rst` — title `Portfolio`
- `docs/agentic_primitives/break_even.rst` — title `Break-Even`
- `docs/agentic_primitives/tools_reference.rst` — title `defipy.tools`
- `docs/agentic_primitives/twin_reference.rst` — title `defipy.twin`
- `docs/agentic_primitives/result_dataclasses.rst` — title `Result Dataclasses`

Template for each (example `position_analysis.rst`):

```rst
.. _agentic_position_analysis:

Position Analysis
====================

*Page content coming in Brief 2 (narrative text) and Brief 3 (autodoc). This stub exists so the toctree builds clean.*
```

Use different ref labels per file (`.. _agentic_<stem>:`) to avoid label collisions — `agentic_position_analysis`, `agentic_price_scenarios`, `agentic_pool_health`, etc.

### 3f. Core Primitives — new index page

**File:** `docs/core_primitives/index.rst`

Create directory `docs/core_primitives/`.

```rst
.. _core_primitives_index:

Core Primitives
====================

*Page content coming in Brief 2 (availability grid + per-operation descriptions). This stub exists so the toctree builds clean.*
```

### 3g. Roadmap & Changelog

**File:** `docs/roadmap.rst`

```rst
.. _roadmap:

Roadmap & Changelog
======================

*Page content coming in Brief 2. This stub exists so the toctree builds clean.*
```

---

## 4. Rewrite the toctree block in `index.rst`

Open `docs/index.rst`. Locate the section at the bottom of the file starting with `.. toctree::` (the first one, `:hidden:` with `Home <self>`). From that line to the end of the file, **replace the entire toctree block** with the block below. Do not touch anything above the first `.. toctree::`.

Replace everything from the first `.. toctree::` line to the end of the file with:

```rst
.. toctree::
    :maxdepth: 1
    :hidden:

    Home <self>


.. toctree::
    :maxdepth: 1
    :hidden:
    :caption: DeFiPy Ecosystem

    book
    courses
    hackathons
    presentations


.. toctree::
    :maxdepth: 1
    :hidden:
    :caption: Getting Started

    quick/index
    installation
    quick/whats_new_v2
    legal


.. toctree::
    :maxdepth: 3
    :hidden:
    :caption: DeFi Math

    math/univ2_math
    math/univ3_math
    math/balancer_math
    math/stableswap_math


.. toctree::
    :maxdepth: 3
    :hidden:
    :caption: Tutorials

    uniswapv2/index
    uniswapv3/index
    balancer/index
    stableswap/index


.. toctree::
    :maxdepth: 2
    :hidden:
    :caption: Agentic DeFi

    agentic/index


.. toctree::
    :maxdepth: 2
    :hidden:
    :caption: State Twin

    twin/index


.. toctree::
    :maxdepth: 1
    :hidden:
    :caption: Core Primitives

    abstract_uniswap
    core_primitives/index


.. toctree::
    :maxdepth: 2
    :hidden:
    :caption: Agentic Primitives

    agentic_primitives/index


.. toctree::
    :maxdepth: 1
    :hidden:
    :caption: Protocol Classes

    primitive_uniswapv2
    primitive_uniswapv3
    primitive_balancer
    primitive_stableswap


.. toctree::
    :maxdepth: 1
    :hidden:
    :caption: Roadmap & Changelog

    roadmap
```

Key changes in this toctree block (for review, do not include this list in the commit):

- "Analytics" caption gone. The old entries (`tutorials/univ2_sim`, `tutorials/order_book`, `tutorials/imp_loss`, `tutorials/fsm`, `tutorials/simple_tree_pt1`, `tutorials/simple_tree_pt2`) disappear from the sidebar. These notebook files remain on disk under `docs/tutorials/` — they are not deleted, they will be re-linked from within the per-protocol `uniswapv2/index.rst` and `uniswapv3/index.rst` tutorial indexes in Brief 2. For Brief 1, they simply stop appearing in the global sidebar.
- "Onchain" caption gone. Files already deleted in Step 1.
- Two `index.ipynb` references are now `.md`-less bare names — check that `math/univ2_math.ipynb` in the old toctree is being replaced with `math/univ2_math` (no `.ipynb` extension). Sphinx will fail to resolve these because notebooks are excluded and we haven't stubbed them. **For Brief 1, also create the following additional stubs:**

**File:** `docs/math/univ2_math.rst`

```rst
.. _univ2_math:

Uniswap V2 Math
====================

*For the full derivation, see the notebook on GitHub:*
`univ2_math.ipynb <https://github.com/defipy-devs/defipy-docs/blob/main/docs/math/univ2_math.ipynb>`_

*Stub pending Brief 2 narrative content.*
```

**File:** `docs/math/univ3_math.rst`

```rst
.. _univ3_math:

Uniswap V3 Math
====================

*For the full derivation, see the notebook on GitHub:*
`univ3_math.ipynb <https://github.com/defipy-devs/defipy-docs/blob/main/docs/math/univ3_math.ipynb>`_

*Stub pending Brief 2 narrative content.*
```

- "Abstract API" caption renamed to "Core Primitives" (captures both the existing `abstract_uniswap` page and the new `core_primitives/index` availability grid page).
- "Primitive API" caption renamed to "Protocol Classes" — the four `primitive_*.rst` filenames are unchanged, so the URLs `/primitive_uniswapv2.html`, etc., keep resolving. This preserves SEO equity per the plan's "never 404 existing URLs" stance.
- New sections appear in order: Agentic DeFi, State Twin, Core Primitives, Agentic Primitives, Protocol Classes, Roadmap.

---

## 5. Gate: local build must succeed

```bash
cd docs
sphinx-build -b html . _build/html 2>&1 | tee /tmp/build_brief1.log
cd ..
```

Scan the log for:

- **Errors** — any line starting with `ERROR:` or `CRITICAL:`. These must be zero.
- **Warnings** — the following warnings are **acceptable** for Brief 1:
  - `document isn't included in any toctree` — we'll get this for the notebook files under `tutorials/` that were in the old Analytics toctree but aren't in the new sidebar (they'll be re-linked in Brief 2). Count them up and note in your summary; should be around 6-10.
  - `toctree contains reference to nonexisting document` — should be zero. If any appear, a stub is missing; fix and re-run.
  - `unknown document` — should be zero.

Any other warning category — flag it in the summary but don't necessarily block on it.

Open `docs/_build/html/index.html` in a browser. Verify:

- Sidebar captions appear in this order: DeFiPy Ecosystem, Getting Started, DeFi Math, Tutorials, Agentic DeFi, State Twin, Core Primitives, Agentic Primitives, Protocol Classes, Roadmap & Changelog
- "Analytics" caption is gone
- "Onchain" caption is gone
- "Abstract API" and "Primitive API" captions are gone (renamed)
- Click through each new stub — they should render with the placeholder text
- The home page body prose above the toctree is unchanged (tagline, intro, Primitive vs Abstract recommendation, Join/Swap object descriptions, Precision table)

---

## 6. Commit structure

Three commits, in order:

**Commit 1** — already done in Step 1 (`chore(docs): drop onchain section and xdm PDF ahead of v2 reorg`).

**Commit 2** — `conf.py` fix and `requirements.txt` pin:

```bash
git add docs/conf.py docs/requirements.txt
git commit -m "fix(docs): resolve nbsphinx/exclude contradiction, pin defipy >= 2.0.0

- Remove nbsphinx from extensions (notebooks excluded for v2.0)
- Single exclude_patterns assignment (removes silent override)
- Bump defipy pin from 0.0.19 to 2.0.0 for autodoc of new v2 modules"
```

**Commit 3** — stubs and toctree rewrite:

```bash
git add docs/
git commit -m "feat(docs): v2 IA skeleton per Option A reorg

- Drop Analytics and Onchain sections from toctree
- Rename Abstract API -> Core Primitives, Primitive API -> Protocol Classes
- Add new sections: Agentic DeFi, State Twin, Agentic Primitives, Roadmap
- Add What's new in v2.0 to Getting Started
- Add Balancer and Stableswap DeFi Math stubs
- Stub all new pages; content lands in Briefs 2 and 3
- Home page body prose unchanged (rewrite deferred to end of Day 6)

URLs preserved: /primitive_uniswapv2.html, /primitive_uniswapv3.html,
/primitive_balancer.html, /primitive_stableswap.html, /abstract_uniswap.html,
and all existing /quick/, /uniswapv2/, /uniswapv3/, /balancer/, /stableswap/,
/tutorials/ paths."
```

Do not push. Ian will review locally before pushing.

---

## 7. Deliverables summary

When Brief 1 is complete, produce a short summary with:

1. Confirmation that the build passed (or didn't — include log excerpt if any unexpected errors)
2. Count of "document isn't included in any toctree" warnings (should be ~6-10 for orphaned tutorials)
3. List of commits made (three, per above)
4. Any deviations from the brief and why
5. Flag for Ian to review locally before push

If anything in this brief is ambiguous or a file edit produces unexpected results, stop and ask rather than improvising. This brief is intentionally conservative — the editorial decisions are all settled, the mechanical work is well-scoped.

---

*End of Brief 1. Brief 2 (narrative content) and Brief 3 (autodoc + deploy) follow after Ian's local review of the skeleton.*
